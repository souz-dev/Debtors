/**
 * Split `Array` elements into every but last and last element.
 * @param value - An Array with at least one item.
 */
declare const splitOnLast: <T>(value: T[]) => [T[], T];
export default splitOnLast;
